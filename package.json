{
  "name": "soft-focus",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.5",
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "smoke:test": "node --import tsx tests/fullFlowSmoke.ts && node --import tsx tests/sessionPersistence.ts && node --import tsx tests/sessionRestartLifecycle.ts",
    "build": "bun run typecheck && vite build",
    "validate": "bun run typecheck && bun run smoke:test && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3",
    "vite": "^8.0.9"
  }
}
