{
  "name": "soft-focus",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "smoke:test": "tsx tests/fullFlowSmoke.ts && tsx tests/sessionPersistence.ts && tsx tests/sessionRestartLifecycle.ts",
    "build": "npm run typecheck && vite build",
    "validate": "npm run typecheck && npm run smoke:test && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3",
    "vite": "^8.0.9"
  }
}
